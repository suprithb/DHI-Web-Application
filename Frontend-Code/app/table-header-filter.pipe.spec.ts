import { TableHeaderFilterPipe } from './table-header-filter.pipe';

describe('TableHeaderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TableHeaderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
